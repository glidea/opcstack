！！！每完成一个任务并且验证通过之后就 commit 一下！！！固定提醒

# Task-001: 用户目录 Admin API

## 描述
新增管理员用户列表与搜索能力，返回控制台用户列表和详情所需的现有字段。查询直接关联 Meta DB 中的用户、分片和内测码数据，不增加用户修改动作。

## 不包含
- 用户编辑、封禁、删除和会话管理
- 用户积分、订阅和邀请奖励详情

## TODO 清单
- [x] 1. 先为管理员鉴权、搜索、分页和空结果补充 E2E 与 Handler 失败测试
- [x] 2. 在 `src/api-contract/` 定义用户列表请求、响应和错误契约，并加入 typed client
- [x] 3. 在 `src/backend/api/handler/` 实现用户、`user_shards`、`d1_shards` 和 `beta_code` 的只读查询
- [x] 4. 在 `src/backend/api/index.ts` 注册 `/api/admin/list_users`
- [x] 5. 跑通类型检查、单元测试和本地 E2E

## 验收测试步骤
1. 使用管理员身份按用户 ID、邮箱和名称分别搜索
2. 确认返回用户基本信息、内测准入、注册来源和分片信息，分页 `total` 正确
3. 使用普通用户或无会话请求，确认返回 401 且不泄露用户数据

# Task-002: 通知历史 Admin API

## 描述
新增管理员通知历史查询，覆盖全站通知和指定用户通知。复用现有通知表和创建通知接口，不读取用户侧通知已读状态。

## 不包含
- 通知编辑、撤回、删除、草稿和定时发布
- 用户通知已读率统计

## TODO 清单
- [x] 1. 先为通知 ID、范围、目标用户、类型、时间筛选和鉴权补充失败测试
- [x] 2. 扩展 `src/api-contract/notifications.ts`，新增 Admin 列表契约和 typed client 方法
- [x] 3. 在通知 Handler 中实现 Meta DB 分页查询并返回目标范围
- [x] 4. 注册 `/api/admin/list_notifications`
- [x] 5. 跑通类型检查、单元测试和本地 E2E

## 验收测试步骤
1. 创建一条全站通知和一条定向通知
2. 使用管理员接口按范围和目标用户筛选，确认历史记录和总数正确
3. 使用普通用户请求管理员列表，确认返回 401

# Task-003: AI 任务 Admin API

## 描述
新增统一的图片、TTS 和视频任务列表与详情查询。接口从所有活跃或 draining 分片读取真实任务字段，并明确返回任务所属分片和对象路径。

## 不包含
- 同步 AI 调用和实时语音记录
- 任务取消、重试、重放和私有文件读取

## TODO 清单
- [x] 1. 先为三类任务合并、筛选、排序、分页、详情和鉴权补充失败测试
- [x] 2. 在 `src/api-contract/` 定义 AI 任务列表与详情的判别联合类型，并加入 typed client
- [x] 3. 实现跨分片任务查询，支持类型、任务 ID、用户、状态、Provider、Model 和时间筛选
- [x] 4. 详情按任务类型返回当前表中已存字段、分片标识和公开或私有对象路径
- [x] 5. 注册 `/api/admin/list_ai_tasks` 和 `/api/admin/get_ai_task`，跑通单元测试与本地 E2E

## 验收测试步骤
1. 准备图片、TTS 和视频任务记录并清除类型筛选
2. 确认统一列表按创建时间排序，类型和状态筛选只返回匹配记录
3. 打开失败任务详情，确认最后错误和已存字段完整，私有结果仅返回对象路径

# Task-004: 运营概览 Admin API

## 描述
新增一个只读运营概览聚合接口，统一返回 PRD 已确认的指标口径。指标只来自 Meta DB 和 Tenant Shard DB 的持久化事实，不引入 Cloudflare 平台指标。

## 不包含
- 活跃用户、留存、成本、毛利和 Queue 积压
- 新增汇总表、Cron 或缓存

## TODO 清单
- [x] 1. 先为时间窗口、空数据、多币种和跨分片汇总补充失败测试
- [x] 2. 定义概览请求、响应和 typed client 契约
- [x] 3. 汇总用户总数与近 7 日新增、近 30 日已支付金额、近 7 日反馈数
- [x] 4. 汇总近 24 小时三类 AI 任务数量、终态完成率和失败数
- [x] 5. 汇总 claimed 兑换码和 disputed 支付数量，注册 `/api/admin/get_overview`
- [x] 6. 跑通类型检查、单元测试和本地 E2E

## 验收测试步骤
1. 调用概览接口并核对每项指标的时间范围
2. 准备两个币种的支付记录，确认金额不被错误相加而是按币种分组
3. 在无任务或无待处理事项时调用，确认返回明确的零值而不是请求失败

# Task-005: 管理控制台壳层

## 描述
在 `/{locale}/admin` 下建立受保护的控制台布局和八个一级路由。壳层复用现有 SvelteKit、`svelte-i18n`、UI primitives 和设计系统，不复制临时 HTML 的实现代码。

## 不包含
- 各业务模块的数据表格和表单
- Public assets 页面和独立 Grant credits 页面

## TODO 清单
- [x] 1. 先写权限、默认路由、八个导航项和语言路径切换的验收用例
- [x] 2. 创建 Admin layout、桌面侧栏、移动端导航和顶部操作区
- [x] 3. 未登录用户跳转登录页，非 `SYSTEM_EMAIL` 用户显示 403 且不渲染控制台菜单
- [x] 4. 接入现有 LocaleSwitcher 语义，补齐英文和中文消息
- [x] 5. 为八个路由提供可运行的 loading 占位并设置页面标题和 canonical
- [x] 6. 使用 In App Browser 验证桌面和移动端路由、权限及语言切换

## 验收测试步骤
1. 分别以未登录、普通用户和超级管理员访问 `/en/admin`
2. 确认前两者看不到管理菜单，管理员默认进入 Overview 并看到八个入口
3. 在任一子路由切换中文，确认仍停留在对应模块且页面没有横向溢出

# Task-006: 运营概览页面

## 描述
实现默认运营概览，展示四组核心指标、三类待处理事项和 AI 任务分布。待处理事项必须能进入对应模块并携带筛选条件。

## 不包含
- 自定义报表和图表配置
- Cloudflare Analytics、日志和告警

## TODO 清单
- [x] 1. 先定义有数据、全零、请求失败和下钻筛选的验收用例
- [x] 2. 接入 `getOverview` 并实现 loading、error 和空待处理状态
- [x] 3. 展示用户、支付、AI 完成率、反馈和最后更新时间
- [x] 4. 展示任务类型分布和可点击的 failed、claimed、disputed 待处理项
- [x] 5. 补齐中英文文案并验证窄屏指标布局

## 验收测试步骤
1. 打开概览并将页面值与接口响应逐项核对
2. 点击失败任务，确认进入 AI 任务页并应用 `failed` 与近 24 小时筛选
3. 使用全零响应，确认显示正常状态而不是空白区域

# Task-007: 用户页面与积分发放

## 描述
实现用户列表、详情抽屉和用户上下文操作。积分发放只从用户详情打开，内部 `source_id` 由页面生成并在不确定重试时复用，任何可见区域都不展示它。

## 不包含
- 独立积分发放路由或导航
- 用户修改、封禁、删除和跨用户私有文件访问

## TODO 清单
- [x] 1. 先定义用户搜索、详情、跨模块跳转、积分成功和不确定重试的验收用例
- [x] 2. 实现用户列表、分页、刷新、复制 ID、错误和 Empty 状态
- [x] 3. 实现用户详情抽屉，展示准入、注册来源、分片和 D1 上下文
- [x] 4. 实现积分发放确认对话框，在组件状态中生成并复用 `source_id`
- [x] 5. 实现定向通知入口及反馈、支付、AI 任务的用户筛选跳转
- [x] 6. 补齐中英文表格、详情、表单和错误文案

## 验收测试步骤
1. 搜索用户并打开详情，确认列表筛选和滚动位置在关闭后保持
2. 发放正数积分，确认成功后显示最新余额且页面从未出现 Source ID
3. 模拟请求结果不确定后重试，确认请求复用同一内部 `source_id`

# Task-008: 内测码页面

## 描述
实现内测码列表、筛选和批量生成闭环。生成结果在对话框中完整展示并支持单个或全部复制。

## 不包含
- 内测码作废、编辑和删除
- 用户内测资格撤销

## TODO 清单
- [x] 1. 先定义筛选、分页、批量生成和复制结果的验收用例
- [x] 2. 接入现有 `listBetaCodes`，实现状态、用户和时间筛选
- [x] 3. 接入 `generateBetaCodes`，增加数量校验和二次确认
- [x] 4. 实现生成结果、复制单个、复制全部和关闭后刷新
- [x] 5. 补齐中英文、loading、error 和 Empty 状态

## 验收测试步骤
1. 按已使用和未使用状态筛选并核对总数
2. 批量生成 20 个内测码，确认结果完整且可复制全部
3. 关闭结果后确认新码出现在刷新后的列表

# Task-009: 兑换码页面

## 描述
实现兑换码列表、状态筛选和批量生成。页面必须清楚区分 `unused`、`claimed` 和 `granted`，其中 `claimed` 使用需要关注的视觉状态。

## 不包含
- 兑换码作废、补发和人工修改状态
- 兑换失败 Saga 的恢复动作

## TODO 清单
- [x] 1. 先定义三态展示、金额和日期筛选、批量生成上限的验收用例
- [x] 2. 接入现有 `listCreditCodes` 并实现全部已支持筛选条件
- [x] 3. 接入 `generateCreditCodes`，实现数量、积分金额和过期时间表单
- [x] 4. 实现单次最多 200 个、二次确认、复制和列表刷新
- [x] 5. 补齐中英文、loading、error 和 Empty 状态

## 验收测试步骤
1. 准备三种状态记录并确认列表视觉和字段完整
2. 按 claimed 筛选，确认展示领取用户、领取时间且发放时间为空
3. 尝试生成 201 个，确认前端阻止提交并给出具体错误

# Task-010: 反馈页面

## 描述
实现跨分片反馈列表和只读详情。页面展示接口已返回的完整内容，不创造处理状态或回复工作流。

## 不包含
- 回复、负责人、处理状态和删除
- 反馈分类配置

## TODO 清单
- [x] 1. 先定义筛选、详情、用户跳转和空结果的验收用例
- [x] 2. 接入现有 `listFeedbacks`，实现用户、类型和时间筛选
- [x] 3. 实现反馈摘要表格与完整内容详情
- [x] 4. 用户 ID 跳转用户页并自动应用筛选
- [x] 5. 补齐中英文、loading、error 和 Empty 状态

## 验收测试步骤
1. 按用户和类型筛选反馈并核对分页结果
2. 打开长内容反馈，确认完整内容可读且没有回复或处理按钮
3. 点击用户 ID，确认用户页已带入对应筛选

# Task-011: 公告通知页面

## 描述
实现通知历史、详情和发布流程。发布支持全站或指定用户两种范围，全站发送必须二次确认。

## 不包含
- 编辑、撤回、删除、草稿和定时发布
- 投递率和已读率统计

## TODO 清单
- [x] 1. 先定义历史筛选、全站确认、定向预填和发布刷新验收用例
- [x] 2. 接入 Admin 通知历史接口并实现列表和只读详情
- [x] 3. 接入现有 `createNotification`，使用分段控件选择发送范围
- [x] 4. 从用户页进入时预填目标用户，全站发布前显示风险确认
- [x] 5. 发布成功后刷新历史并补齐中英文、error 和 Empty 状态

## 验收测试步骤
1. 从通知页发布全站公告，确认二次提示明确说明影响所有用户
2. 从用户详情发布通知，确认范围和目标用户已自动选中
3. 发布成功后按目标用户筛选，确认历史列表出现新记录

# Task-012: 支付页面

## 描述
实现只读支付交易列表和详情，复用现有 Admin 支付接口。金额按币种格式化，原始最小货币单位保留在详情中。

## 不包含
- 退款、争议裁决和商品配置
- Checkout order、订阅和 Webhook 事件详情

## TODO 清单
- [x] 1. 先定义用户、类型、状态筛选和争议详情验收用例
- [x] 2. 接入 `listAdminPaymentTransactions` 并实现分页和筛选
- [x] 3. 实现金额、币种、状态和已发积分的列表展示
- [x] 4. 实现只读详情和用户上下文跳转
- [x] 5. 补齐中英文、loading、error 和 Empty 状态

## 验收测试步骤
1. 按 disputed 状态筛选并打开交易详情
2. 确认格式化金额、原始金额、币种、争议时间和已发积分正确
3. 确认页面不存在退款、争议处理或商品修改动作

# Task-013: AI 任务页面与 Cloudflare 外链

## 描述
实现三类 AI 任务统一列表、按类型展开的详情和上下文 Cloudflare 外链。链接只由现有 Account ID、D1 Database ID、Queue 名称、R2 Bucket 名称和 Worker 名称构造。

## 不包含
- 任务取消、重试、重放和参数修改
- 私有结果预览或下载
- Cloudflare 指标复制到控制台

## TODO 清单
- [x] 1. 先定义三类筛选、失败详情、处理中刷新、公开与私有结果验收用例
- [x] 2. 接入 AI 任务列表和详情接口，保持筛选、页码和详情状态
- [x] 3. 按任务类型展示真实已存字段和只读 JSON 数据
- [x] 4. 公共结果提供打开与复制，私有结果仅提供对象路径复制
- [x] 5. 复用现有 AI Queue 常量构造 D1、Queue、R2 和 Worker Dashboard 外链
- [x] 6. 本地资源标识无效时隐藏链接，补齐中英文、error 和 Empty 状态

## 验收测试步骤
1. 清除类型筛选后确认图片、TTS 和视频任务同时出现
2. 打开失败任务，确认最后错误、D1 和 Queue 外链正确
3. 分别检查公开和私有结果，确认私有对象没有预览或下载动作
4. 点击处理中任务刷新，确认只重新读取数据库而不触发重试

# Task-014: 控制台跨模块验收

## 描述
完成 P0 的跨模块串联、视觉一致性和端到端验收。只修复验收中发现的问题，不在此任务追加新功能或新抽象。

## 不包含
- P1 用户支持、支付运维、任务恢复和 RBAC
- Public assets 页面
- 性能优化、缓存和高并发设计

## TODO 清单
- [x] 1. 按 PRD 第 17 节逐条执行权限、导航、列表、详情和动作验收
- [x] 2. 验证用户到反馈、通知、支付和 AI 任务的筛选跳转
- [x] 3. 验证中英文切换保持当前路由、筛选、页码和详情状态
- [x] 4. 使用 In App Browser 检查桌面与移动端，不允许全局横向溢出或内容重叠
- [x] 5. 运行 `pnpm test` 和 `pnpm test:e2e`，修复所有回归
- [x] 6. 核对控制台没有 Grant credits 和 Public assets 一级入口
- [x] 7. 用户筛选和通知目标统一使用用户搜索选择器，不要求运营人员手填用户 ID

## 验收测试步骤
1. 以超级管理员完整走通概览、用户、两类码、反馈、通知、支付和 AI 任务
2. 以未登录和普通用户访问每个管理路由，确认无法看到管理数据
3. 在英文和中文下分别完成一次用户积分发放和全站通知发布
4. 在桌面和 390px 移动视口检查全部页面，确认表格局部滚动且页面无整体横向滚动
5. 在反馈、公告、支付和 AI 任务页面按邮箱或名称选择用户，确认页面内部正确应用用户筛选
6. 确认全部测试通过后提交最终 P0 变更

# Task-015: 控制台体验审查与上线

## 描述
按 Impeccable 双阶段审查结果收敛管理控制台的信息架构和运营可读性，完成回归验证后推送并部署生产环境。

## 不包含
- 新增 Admin API 或运营能力
- Public assets 页面
- 为美化而重做现有设计系统

## TODO 清单
- [x] 1. 完成独立设计审查、静态规则扫描和浏览器证据采集
- [x] 2. 将类型、状态等有限枚举筛选改为选择器
- [x] 3. 对高密度筛选使用渐进披露并提供清除筛选
- [x] 4. 格式化积分和技术字段，保证用户身份与操作列可扫读
- [x] 5. 删除“管理控制台”等重复文案并同步设计约束
- [x] 6. 重构导航层级、列表访问状态和图标化行操作，降低扫描噪声
- [x] 7. 在顶栏、用户分片和 AI 任务旁补齐 Worker、D1 和 Queues 上下文外链
- [x] 8. 完成桌面、移动端、双主题和中英文浏览器验收
- [x] 9. 运行单测与端到端测试并修复回归
- [x] 10. 提交、推送并部署生产环境，验证线上路由

## 验收测试步骤
1. 检查所有类型和状态筛选，确认有限枚举不再要求手工输入
2. 打开兑换码、公告和 AI 任务页，确认常用筛选优先且高级筛选可展开、计数和清除
3. 在 1280px 与 390px 视口检查表格，确认用户身份和行操作可发现
4. 切换中英文与明暗主题，确认文案、状态和控件没有溢出或重叠
5. 推送后执行生产部署并访问 `/zh/admin/overview` 验证线上版本
6. 在生产环境确认顶栏 Worker 日志、用户 D1 分片和 AI 任务 Queues 均直达对应 Cloudflare 资源

# Task-016: 控制台布局与信息层级重构

## 描述
统一管理控制台的页面栅格、标题基线、筛选工具栏、表格容器和概览信息层级。复用现有 UI primitives 与业务功能，不照搬临时原型样式。

## 不包含
- 新增或修改 Admin API
- 新增运营功能和数据指标
- 重做非管理后台页面

## TODO 清单
- [x] 1. 先补充页面布局一致性与概览层级的失败测试
- [x] 2. 统一壳层、侧栏、页面宽度、间距和标题操作区
- [x] 3. 将七个列表页的筛选器收敛为紧凑工具栏并统一表格容器
- [x] 4. 将概览重构为指标条、待处理工作台和业务分布
- [x] 5. 将管理后台布局规则同步到 `DESIGN.md`
- [x] 6. 完成桌面、移动端和中英文视觉验收并保存新版宣发截图
- [x] 7. 运行测试、提交、推送并部署生产环境

## 验收测试步骤
1. 在 1440px 视口依次打开八个管理页，确认标题、操作区、筛选器和表格左右边界严格对齐
2. 检查七个列表页，确认常用筛选和查询操作位于同一工具栏，高级筛选展开后不破坏基线
3. 检查概览页，确认核心指标形成单一指标条，待处理事项为首要工作区，没有四张同权重卡片
4. 在 390px 视口检查全部页面，确认筛选器自然换行、表格局部滚动且页面无整体横向滚动
5. 切换中英文，确认标题、按钮和筛选字段没有重叠或截断

# Task-017: 管理控制台使用文档

## 描述
补齐管理控制台面向模板使用者的中英文操作指南，并同步相关文档入口和路线图。

## 不包含
- 修改管理控制台功能或 Admin API
- 复制 Cloudflare 平台操作手册
- 新增 Public assets 页面

## TODO 清单
- [x] 1. 核对八个管理页面、权限和 Cloudflare 外链的现有行为
- [x] 2. 新增模板文档及中英文公开使用指南
- [x] 3. 更新文档首页、快速开始和相关模块交叉链接
- [x] 4. 更新 README 路线图并删除 Agent Runtime 路线
- [x] 5. 更新 AGENTS.md 文档维护约束
- [ ] 6. 运行文档测试与构建，提交并推送

## 验收测试步骤
1. 中英文文档站均能访问 `/docs/guides/admin-console`
2. 使用指南准确覆盖权限、八个页面、核心操作、Cloudflare 跳转和当前限制
3. 文档首页、快速开始、认证、运营、积分、支付、AI 均能找到管理台指南
4. README 不再包含 Agent Runtime 路线，管理台路线标记为完成
5. 文档测试和生产构建通过
